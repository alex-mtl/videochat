// Import mediasoup-client
const mediasoupClient = require("mediasoup-client");

// Export for use in other files
module.exports = {
    mediasoupClient,
    // You can also export other shared dependencies here
};